# وثيقة العمارة — منصة وحي (Wahy)
## Foundation Release — الإصدار الأساسي

هذه الوثيقة تغطي فقط ما تم بناؤه فعليًا (نص المصحف + تصفح + بحث)، وليس
النطاق الكامل للمنصة الموصوفة في مستند الـ 240 قسمًا.

---

## 1. نطاق هذا الإصدار

| المكوّن | الحالة |
|---|---|
| نص القرآن (حفص عن عاصم) | ✅ مبني |
| تصفح حسب السورة | ✅ مبني |
| بحث نصي | ✅ مبني (بحث بسيط، غير منزّه من التشكيل) |
| توثيق المصدر (Provenance) | ✅ مبني |
| Offline بالكامل | ✅ مبني (قاعدة بيانات مضمّنة) |
| الصوت / التلاوة | ❌ غير مبني |
| التجويد المرئي/الصوتي | ❌ غير مبني |
| القراءات العشر | ❌ غير مبني |
| التعرف الصوتي (ASR) | ❌ غير مبني |
| النشر على المتاجر | ❌ غير مبني |

---

## 2. اختيار المنصة التقنية

**Flutter (Dart)** — كود مصدري واحد لأندرويد وiOS.

**سبب الاختيار:**
- يلبي متطلب "تطبيق واحد لكلا المنصتين" مباشرة
- دعم ممتاز لـ RTL والخطوط العربية
- `sqflite` يوفر قاعدة بيانات SQLite محلية كاملة بدون أي اعتماد على إنترنت
- مسار نمو واضح لإضافة الصوت (`just_audio`)، والتحليل الصوتي لاحقًا
  (عبر native plugins أو on-device ML) دون إعادة كتابة الطبقة الأساسية

**بديل تم استبعاده:** تطبيقان منفصلان (Kotlin + Swift) — تم استبعاده لأنه
يضاعف تكلفة الصيانة دون فائدة واضحة في هذه المرحلة المبكرة؛ يبقى خيارًا
لاحقًا إذا احتاج محرك ASR/تجويد وصولًا عميقًا لواجهات native الصوتية غير
المتاحة بسهولة عبر Flutter.

---

## 3. طبقة البيانات

```
assets/db/quran.db  (مضمّن داخل التطبيق، ~2.1 MB)
        │
        ▼ (نسخة عند أول تشغيل)
Application Documents Directory / quran.db  (قابل للقراءة/الكتابة)
        │
        ▼
QuranDatabase (lib/db/quran_database.dart)
        │
        ▼
Models: Surah, Ayah  →  Screens: Home, Surah, Search, About
```

**لماذا النسخ عند أول تشغيل؟** SQLite لا يمكنه فتح ملف داخل حزمة الأصول
(asset bundle) للقراءة والكتابة مباشرة على أغلب المنصات؛ لذلك يُنسخ مرة
واحدة إلى مجلد قابل للكتابة، وهذا نمط قياسي في تطبيقات Flutter/SQLite.

### مخطط قاعدة البيانات (Schema)

انظر `docs/data-model.md` للتفاصيل الكاملة.

جداول: `surahs`, `ayat`, `data_provenance`

**كل صف في جدول `ayat` يحمل حقول Provenance مباشرة** (riwayah, source_primary,
source_rasm_reference, verification_status) — تطبيقًا لمبدأ القسم 11
("كل وحدة بيانات يجب أن تحمل مصدرها وحالة تحققها").

---

## 4. استراتيجية العمل Offline

- التطبيق **لا يحتاج إنترنت أبدًا** لعرض النص — القاعدة كاملة مضمّنة من
  اليوم الأول (يلبي بوابة القسم 230: Offline Release Gate)
- لا يوجد أي استدعاء شبكة في الكود الحالي إطلاقًا
- التوسعات المستقبلية (تحديث بيانات، تنزيل ملفات صوتية) يجب أن تتبع نمط:
  "تعمل بدون إنترنت أولًا، ثم تتحسن إذا توفر" (progressive enhancement)
  وليس العكس

---

## 5. القيود المعروفة في هذا الإصدار (Known Limitations)

1. **البحث النصي حساس للتشكيل** — البحث الحالي مطابقة سلسلة نصية مباشرة
   (LIKE) على النص المُشكَّل بالكامل؛ يحتاج فهرس نص منزّه من التشكيل
   لتجربة بحث حقيقية (خطوة تالية موثقة في `data-model.md`)
2. **لا يوجد اختبار فعلي (untested)** — لم يُشغَّل الكود على أي جهاز أو
   محاكي حقيقي؛ انظر التنبيه في `README.md`
3. **رواية واحدة فقط** (حفص عن عاصم) — البنية جاهزة لإضافة روايات أخرى عبر
   حقل `riwayah` لكن لا توجد بيانات لروايات أخرى بعد
4. **لا رقم نسخة إصدار Tanzil موثّق داخل الكود** — يُنصح بإضافة رقم إصدار
   دقيق (version pin) لملف مصدر Tanzil عند الانتقال للإنتاج، لضمان قابلية
   إعادة البناء (reproducibility)

---

## 6. سجل القرارات المعمارية (ADR مختصر)

| القرار | البديل المرفوض | السبب |
|---|---|---|
| Flutter لكود موحّد | React Native | دعم أنضج للطباعة العربية المعقدة والخطوط القرآنية المتخصصة |
| SQLite مضمّن كامل | تنزيل البيانات عند أول تشغيل | يضمن العمل Offline من اللحظة الأولى دون أي اعتماد على شبكة عند التثبيت |
| Tanzil كمصدر أساسي | استخراج بيانات من تطبيقات وحي/آيات | تجنّب مخاطر ترخيص إعادة التوزيع؛ Tanzil مرخّص صراحة لهذا الغرض |
